# ===========================================================
# Conexiaa Backend - Pinned Python Requirements
# -----------------------------------------------------------
# Install with: pip install -r requirements.txt
# If using uv: uv pip install -r requirements.txt
# ===========================================================

# Core Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database & ORM (PostgreSQL + PGVector)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
pgvector==0.3.5

# Data Validation & Settings
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# HTTP Client (external API calls)
httpx==0.28.1

# Redis (caching / background tasks)
redis[hiredis]==5.2.1
hiredis==2.4.0

# Background Task Queue (optional, for long-running jobs)
celery==5.4.0

# NLP / RAG
sentence-transformers==3.3.1
torch==2.5.1
faiss-cpu==1.9.0.post1   # or faiss-gpu if you have CUDA

# Data manipulation
numpy==1.26.4
pandas==2.2.3
pyarrow==17.0.0   # for parquet support

# ElevenLabs SDK
elevenlabs==1.6.0

# Utilities
orjson==3.10.11
python-multipart==0.0.18

# Development (optional, comment out if not needed)
# pytest==8.3.4
# pytest-asyncio==0.24.0
# httpx-ws==0.3.1

# ===========================================================
# Testing
# ===========================================================
# 1. Create a virtual environment:
#    python3.12 -m venv venv && source venv/bin/activate
# 2. Install dependencies:
#    pip install -r requirements.txt
# 3. Run the backend:
#    uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
# 4. All backend endpoints will be available; RAG and AI services
#    fall back to simulated responses if API keys are not set.